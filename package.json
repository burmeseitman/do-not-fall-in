{
  "name": "dont-fall-in",
  "version": "1.0.0",
  "private": true,
  "description": "Guide a pencil across a paper full of holes without falling through.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
